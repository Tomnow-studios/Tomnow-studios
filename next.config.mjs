/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  output: "export",
  basePath: "/Tomnow-studios",
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
